# Project-scoped Codex configuration for the quality workflow.
# Keep the parent model selection user-controlled; the workflow expects Sol High.

[agents]
enabled = true
default_subagent_model = "gpt-5.6-luna"
default_subagent_reasoning_effort = "high"
max_depth = 1
