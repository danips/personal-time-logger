{
  "name": "personal-time-logger",
  "private": true,
  "type": "module",
  "description": "Local-first time tracking with Google Sheets sync. Packaging and tests only; the extension itself has no dependencies.",
  "engines": {
    "node": ">=20.0.0",
    "npm": ">=8.0.0"
  },
  "scripts": {
    "test": "node --test \"test/*.test.js\"",
    "test:browser": "node scripts/browser-runtime-smoke.mjs",
    "lint:js": "eslint .",
    "lint:extension": "node scripts/prepare-firefox-release.mjs --base-url https://example.invalid/personal-time-logger --output web-ext-artifacts/lint-source && web-ext lint --source-dir web-ext-artifacts/lint-source --self-hosted",
    "lint": "npm run lint:js && npm run lint:extension",
    "build:xpi": "node scripts/build-xpi.mjs"
  },
  "devDependencies": {
    "@eslint/js": "10.0.1",
    "acorn": "8.18.0",
    "eslint": "10.9.1",
    "globals": "17.11.0",
    "web-ext": "10.6.0"
  }
}
